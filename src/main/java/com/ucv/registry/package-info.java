/**
 * Forge 1.21.x deferred registers (items, blocks, entities, sounds, menus, particles, tabs).
 * Add content through the {@code Mod*} helpers; do not construct duplicate DeferredRegisters.
 */
package com.ucv.registry;
